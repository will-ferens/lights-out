import React from "react";
import styled from "styled-components";

const HeaderTitle = styled.h1`
  font-size: 42px;
  letter-spacing: 2px;
`;
const Header = () => {
  return <HeaderTitle>Lights Out</HeaderTitle>;
};

export default Header;
